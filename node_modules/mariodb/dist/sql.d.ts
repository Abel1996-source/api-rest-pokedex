import ExecutionContext from "./context";
export default class SQL {
    private context;
    private strings;
    private keys;
    statement: string;
    params: any[];
    constructor(context: ExecutionContext | null, strings: string[], keys: any[]);
    append(sql: SQL): void;
    prepend(sql: SQL): void;
    run(context: ExecutionContext | null): Promise<any>;
    rows(context: ExecutionContext | null): Promise<any[]>;
    row(context: ExecutionContext | null): Promise<any>;
    value(context: ExecutionContext | null): Promise<any>;
}
